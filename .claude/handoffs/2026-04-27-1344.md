# Handoff — 2026-04-27 13:44

## Session goal
Ship **Codex Entry 3 — "Eight Friends at the Buffet"** (multi-head attention) end-to-end: write, illustrate, verify, commit, push, PR.

## What shipped

### Entry 3 — "Eight Friends at the Buffet"
- File: `content/posts/codex/eight-friends-at-the-buffet.mdx`
- URL: `/posts/eight-friends-at-the-buffet`
- Scope: Rung 4 of the analogy reference — multi-head attention via the four-cravings-for-cat opening, eight parallel buffet lines, 24 chefs at the entrance, Master Chef ($W^O$). Centerpiece: the parameter-count table showing 1,048,576 params either way (single head $d_{model}=512$ vs eight heads × $d_k=64$). Implementation note: production code uses one fat $W^Q$ reshaped, not eight small projections.
- `featured: true` — takes the homepage hero slot. Entry 2 swapped to `featured: false`.
- Closes pointing to Entry 4 ("The Express Lane") and the eventual RoPE entry.

### Three FLUX.1 Dev illustrations
- `eight-friends-at-the-buffet.png` (hero, seed 8848, 1200×624) — eight guests at a long buffet, distinct intents
- `eight-buffet-lines.png` (inline-1, seed 8181, 1216×800) — top-down view of eight parallel buffet lines
- `master-chef-tray.png` (inline-2, seed 9292, 1216×800) — chef's hands transferring portions from eight small plates to one larger plate

All three landed in one shot (no regen iterations needed).

### Editorial note worth keeping
- **Original opening mixed two POVs.** First sentence put the reader at the buffet ("Imagine you walk into the potluck"), then the next paragraph swapped to *cat* as the guest. Akshay caught it immediately. **Lesson:** in any Codex entry, pick one POV per opening and stick with it. The fix kept *cat* as the guest from sentence one.
- Worth applying to future entries during the draft pass, not just at review.

### Commit + PR
- Commit: `content(codex): add third entry — Eight Friends at the Buffet` (also bundles the prior session's handoff which missed PR #22, and the seat-ribbons featured-flag flip).
- PR #23: https://github.com/akshayb7/ai-blog/pull/23

### Verification
- 119/119 unit tests green
- Local server confirms `/posts/eight-friends-at-the-buffet`, `/posts/seat-ribbons`, `/posts/attention-is-a-potluck`, `/category/codex` all return 200
- Featured slot picks up Entry 3 on homepage

## Image-generation pattern observed
- Entry 1 (kale plate) needed ~5 regen iterations.
- Entry 2 (six ribbons) needed multiple seeds + explicit count in negative prompt.
- Entry 3 landed all three in first try with detailed multi-clause prompts and earthy-palette specifics. The pattern: **front-load the explicit count, the layout, and the palette in the first sentence** of the prompt; reserve later sentences for atmosphere. Keep negative prompts focused on count failures and obvious render artifacts.

## Next up
**Entry 4 — "The Express Lane"** (stacking + residuals + fresh Q/K/V per block). Material in `research/attention-is-a-potluck/analogy-reference.md` Rungs 5 & 6. Centerpiece: the shapes table for what flows between blocks — Q/K/V are derived fresh per block, nothing is inherited. Closer should tease the KV cache.

After that:
- RoPE deep dive (own entry — promised in Entry 2)
- First teardown: Gemma 4

## Known issues (unchanged)
- Newsletter still non-functional (`alert('coming soon!')`)
- `'use client'` on Hero may be removable (imports server-safe `lib/categories`) — verify
- Dark mode toggle in nav — verify it works end-to-end

## State
- Branch: `content/eight-friends-at-the-buffet`, 1 commit ahead of `main` via PR #23
- Working tree: clean except `.claude/scheduled_tasks.lock` (untracked, ignore)
- Dev server may still be running at http://localhost:3000 (started earlier in session)
