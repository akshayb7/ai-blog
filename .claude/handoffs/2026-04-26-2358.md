# Handoff — 2026-04-26 23:58

## Session goal
Ship **Codex Entry 2 — "The Seat Ribbons"** end-to-end: write, illustrate, verify, commit, push, PR.

## What shipped

### Entry 2 — "The Seat Ribbons" (live on `transformers` branch)
- File: `content/posts/codex/seat-ribbons.mdx`
- URL: `/posts/seat-ribbons`
- Scope: absolute sinusoidal positional encoding only. Opens with the *cat sat on the mat / mat sat on the cat* asymmetry. Walks through ribbons → why integers fail (magnitude + distance) → sinusoidal wave trick → math → the training-length crack that motivates RoPE.
- GPT pre-pass edits applied: softened overclaims, sharpened the 513-ribbon framing, harder RoPE transition, added closer (*Without ribbons, attention sees a pile. With them, it sees a sentence.*).
- `featured: true` — Entry 2 takes the homepage hero slot. Entry 1 swapped to `false`.

### Three illustrations
- `seat-ribbons-v3.png` (hero, FLUX.1 Dev, vertical curled fabric ribbons in honey/yellow/pink/peach/cream)
- `ribbons-on-the-table-v4.png` (inline-1, FLUX.1 Dev, seed 6006 — six ribbons exactly, after a Klein-C reject and the v3 Dev backup also being too cluttered)
- `wave-dials-v2.png` (inline-2, FLUX.2 Klein-A, dramatic frequency progression top→bottom)

### Cache-busting strategy
Browser/Next image-cache wouldn't refresh on overwrite. Solved by versioning filenames per regen (`-v2`, `-v3`, `-v4`) and updating MDX refs. **All 7 Codex images now live under their final versioned names** — no orphans, no `_backup-dev/` folder.

### Commits + PR
1. `content(codex): version Entry 1 image filenames to bust browser cache` — renamed Entry 1's four images to `-v2.png`, MDX updated.
2. `content(codex): add second entry — The Seat Ribbons` — MDX, three images, PROGRESS update, prior session's handoff included.

PR #22: https://github.com/akshayb7/ai-blog/pull/22 (against `main`)

### Verification
- 119/119 unit tests green
- Production build clean — `/posts/seat-ribbons` and `/posts/attention-is-a-potluck` both prerender, `/category/codex` prerenders
- User signed off on final image set

## Image-generation notes worth keeping
- **FLUX.1 Dev > FLUX.2 Klein for editorial-quality ribbon illustrations.** Klein produces faster but less polished output; for hero/inline-1 quality the user prefers Dev despite ~5min generation time.
- **Klein is fine for abstract pattern illustrations** like wave-dials where exact rendering doesn't matter.
- **Ribbon counts**: explicit count in negative prompt ("seven ribbons, eight ribbons, five ribbons, extra ribbons") + positive count emphasis ("Six ribbons total, no more no less") landed on six in seed 6006. Prior seeds gave 5 or 7.

## Next up
**Entry 3 — "Eight Friends at the Buffet"** (multi-head attention). Centerpiece: the parameter-count "aha" — 8 heads of dim 64 has identical params to 1 head of dim 512 (1,048,576 each). Material in `research/attention-is-a-potluck/analogy-reference.md` Rung 4. Target 8–10 min standalone.

After that:
- Entry 4: "The Express Lane" (stacking + residuals + fresh Q/K/V per block)
- RoPE deep dive (own entry — promised in Entry 2's closer)
- First teardown: Gemma 4

## Known issues (unchanged from last handoff)
- Newsletter still non-functional (`alert('coming soon!')`)
- `'use client'` on Hero may be removable now (imports server-safe `lib/categories`)
- Dark mode toggle present in Navigation; verify it works end-to-end

## State
- Branch: `transformers`, 2 commits ahead of `main` via PR #22
- Working tree: clean except `.claude/scheduled_tasks.lock` (untracked, ignore)
